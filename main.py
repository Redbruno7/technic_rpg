from interface.tela_principal import tela, largura, altura, fonte, botoes, cursores
from interface.tela_principal import janela_principal


if __name__ == "__main__":
    janela_principal(tela, largura, altura, fonte, botoes, cursores)
